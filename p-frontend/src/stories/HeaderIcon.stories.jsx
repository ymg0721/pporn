import React from 'react';

import { HeaderIcon } from '../component/HeaderIcon';

export default {
  title: '/HeaderIcon',
  component: HeaderIcon,
};

const Template = () => <HeaderIcon  />;

export const Icon = Template.bind({});
