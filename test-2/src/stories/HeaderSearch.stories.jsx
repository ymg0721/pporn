import React from 'react';

import { HeaderSearch } from '../component/HeaderSearch';

export default {
  title: '/HeaderSearch',
  component: HeaderSearch,
};

const Template = () => <HeaderSearch  />;

export const Search = Template.bind({});
